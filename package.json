{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna version patch --yes --exact --force-publish=@tencent/ygy-lerna-1 && lerna publish from-git --yes"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
